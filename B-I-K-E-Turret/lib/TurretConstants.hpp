#pragma once

// Control Constants
constexpr float STEPS_PER_REV = 8000.0f;
constexpr float MAX_RPM = 100.0f;

// Motor Constants
constexpr float TURRET_OUTPUT_GEAR_RATIO = 1.0f;
constexpr float TURRET_STEP_PIN = 0;
constexpr float TURRET_DIR_PIN = 1;

/* DC Motor stuff */

/* Sensor stuff */